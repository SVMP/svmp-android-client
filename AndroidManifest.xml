<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="org.mitre.svmp.client"
          android:versionCode="6"
          android:versionName="1.3.2">

    <uses-sdk android:minSdkVersion="13" android:targetSdkVersion="19"/>

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <!-- used for performance measurements -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>

    <application android:label="@string/app_name">
        <!-- Allows for intercepting URL intents. -->
        <activity android:name=".SendNetIntent"
                  android:label="@string/app_name"
                  android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
                  android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:host="*" android:scheme="http"/>
            </intent-filter>
        </activity>

        <activity android:name="org.mitre.svmp.ConnectionDetails"
                android:label="@string/connectionDetails_title"
                android:windowSoftInputMode="adjustResize"/>

        <activity android:name="org.mitre.svmp.ConnectionList"
                  android:label="@string/connectionList_title">
            <intent-filter android:label="@string/app_name">
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name="org.mitre.svmp.SvmpPreferences"
                android:label="@string/preferenceScreen_title"/>

        <activity android:name="org.mitre.svmp.SvmpActivity"/>

        <activity android:name="org.mitre.svmp.AppRTCDemoActivity"
                  android:label="@string/app_name"
                  android:configChanges="orientation|screenSize"
                  android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen"/>

        <!-- Memorizing Trust Manager library integration -->
        <activity android:name="de.duenndns.ssl.MemorizingActivity" />
    </application>
</manifest>
